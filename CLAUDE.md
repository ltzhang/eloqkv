When building EloqKV, just do `make -j 6` under the build/ directory. 

The tigerbeelt API and usage documentation is available at https://docs.tigerbeetle.com/. Under `external/tigerbeetle` has a checked out source code of tigerbeetle. This is the Golden version that provide the ground trueth for the expected behavior. 

