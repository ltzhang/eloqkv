When building EloqKV, just do `make -j 6` under the build/ directory. 

The tigerbeelt API and usage documentation is available at https://docs.tigerbeetle.com/. Under `external/tigerbeetle` has a checked out source code of tigerbeetle. This is the Golden version that provides the ground trueth for the expected behavior. 

lua_beetle should be checked in and pushed to its own repo.

eloqkv should be checked in and pushed to eloq_beetle branch. 